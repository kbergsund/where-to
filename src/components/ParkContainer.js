import ActivityForm from './ActivityForm';

const ParkContainer = () => {
  return(
    <section>
      <ActivityForm />
      <section>parks</section>
    </section>
  )
}

export default ParkContainer